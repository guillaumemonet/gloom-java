// Projet Gradle INDÉPENDANT du build desktop (../build.gradle) : le plugin Android et ses
// dépôts ne s'invitent pas dans un build qui marche. Les sources du moteur sont partagées
// telles quelles via un sourceSet pointant sur ../src (cf. build.gradle).
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'gloom-android'
